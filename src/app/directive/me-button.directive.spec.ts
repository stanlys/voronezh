import { MeButtonDirective } from './me-button.directive';

describe('MeButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new MeButtonDirective();
    expect(directive).toBeTruthy();
  });
});
